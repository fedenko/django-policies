============
Installation
============

At the command line::

    $ easy_install django-policies

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-policies
    $ pip install django-policies
